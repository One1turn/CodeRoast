# CodeRoast 示例：一段该被烤的代码
import os

def do_stuff(a, b, c):
    x = 100
    y = 200
    tmp = a + b + c * x + y * 999
    if tmp > 0:
        if tmp > 100:
            if tmp > 1000:
                if tmp > 10000:
                    if tmp > 100000:
                        print("deep")
    return tmp

password = "admin123"
api_key = "sk-secret-12345"

data = eval(input())
result = data
result = data
result = data

# TODO: fix this later
# FIXME: this is broken
pass
